[
    {
        "type": "function",
        "function": {
            "name": "analyze_files",
            "description": "Reads index.html, styles.css, and script.js files and returns their contents for analysis",
            "parameters": {
                "type": "object",
                "properties": {
                    "args": {
                        "type": "string",
                        "description": "Optional arguments (not used, can be empty string)"
                    }
                },
                "required": ["args"]
            }
        }
    },
    {
        "type": "function",
        "function": {
            "name": "write_plan",
            "description": "Writes content to research.md file (overwrites if exists)",
            "parameters": {
                "type": "object",
                "properties": {
                    "content": {
                        "type": "string",
                        "description": "The content to write to plan.md"
                    }
                },
                "required": ["content"]
            }
        }
    }
]